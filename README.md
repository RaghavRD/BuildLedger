# BuildLedger 🏗️

A construction money management system.

## 🚀 How to Start the App

1.  **Start the Development Server**
    ```bash
    npm run dev
    ```
    Open [http://localhost:3000](http://localhost:3000) in your browser.

2.  **View the Database (Optional)**
    To see the users and data inside your SQLite database:
    ```bash
    npx prisma studio
    ```
    This opens a visual editor at [http://localhost:5555](http://localhost:5555).

## 🛠️ Features
-   **Local Database**: SQLite (`dev.db`). No setup required.
-   **Authentication**: Secure Email/Password login.
-   **Dashboard**: Track projects and expenses.
